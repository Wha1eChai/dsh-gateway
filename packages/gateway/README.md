# @dshapps/dsh-gateway

Host and browser entrypoints for the dsh-gateway integration. Phase 1 freezes
the installable package and DSH client-module shape; runtime behavior begins in
later compatibility-gated phases.

